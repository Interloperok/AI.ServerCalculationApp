[build-system]
requires = ["hatchling>=1.29.0"]
build-backend = "hatchling.build"

[project]
name = "ai-server-calculator-backend"
version = "1.3.0"
description = "Backend API for AI/LLM server sizing calculations."
readme = "README.md"
requires-python = ">=3.14,<3.15"
license = { text = "MIT" }
authors = [{ name = "AI Server Calculator Maintainers" }]
dependencies = [
  "apscheduler==3.11.2",
  "fastapi==0.137.0",
  # Pinned >=6.1.0 to avoid CVE-2026-41066 (lxml 6.0.x XML external entity).
  "lxml>=6.1.1",
  "openpyxl>=3.1.0",
  "pandas>=3.0.3",
  "pydantic>=2.13.4,<3",
  "requests==2.34.2",
  "uvicorn==0.49.0",
]

[dependency-groups]
dev = [
  "coverage==7.14.1",
  "httpx==0.28.1",
  "hypothesis==6.155.2",
  "mypy==2.1.0",
  # Transitive via pytest; pinned >=2.20.0 to avoid CVE-2026-4539.
  "pygments>=2.20.0",
  # 9.0.3 fixes CVE-2025-71176.
  "pytest==9.1.0",
  "pytest-cov==7.1.0",
  "ruff==0.15.17",
  "ty==0.0.49",
  "types-requests==2.33.0.20260513",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py314"
line-length = 100
extend-exclude = ["__pycache__"]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]

[tool.ty.rules]
deprecated = "ignore"

[[tool.ty.overrides]]
include = ["main.py", "models/sizing.py", "tests/**"]

[tool.ty.overrides.rules]
all = "ignore"

[tool.mypy]
python_version = "3.14"
warn_unused_configs = true
exclude = "(^|.*/)(models/sizing\\.py|tests/.*)$"

[[tool.mypy.overrides]]
module = ["models.sizing"]
ignore_errors = true

[[tool.mypy.overrides]]
module = ["apscheduler.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["pandas", "pandas.*", "openpyxl", "openpyxl.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]

[tool.coverage.run]
branch = true
source = ["."]
# Temporary strict scope: coverage gates currently exclude legacy non-M39 domains
# to enforce strictness on redesigned pipeline and sizing-critical modules first.
omit = [
  "tests/*",
  "services/auto_optimize_service.py",
  "services/gpu_catalog_service.py",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
